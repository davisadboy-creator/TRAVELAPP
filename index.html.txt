<!DOCTYPE html> <html lang="en"> <head> <meta charset="UTF-8" /> <meta name="viewport" content="width=device-width, initial-scale=1.0" /> <title>Travel App - No Node Version</title> <!-- React + ReactDOM via CDN --> <script crossorigin src="https://unpkg.com/react@18/umd/react.development.js"></script> <script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script> <!-- Babel for JSX support --> <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script> <style> body { margin: 0; font-family: Arial, sans-serif; background: #0d0d0d; color: white; padding: 20px; } .container { max-width: 500px; margin: auto; } h1 { font-size: 28px; margin-bottom: 10px; } input, button { width: 100%; padding: 14px; border-radius: 14px; border: none; margin-top: 12px; font-size: 16px; } input { background: #1c1c1c; color: white; } button { background: #ffd500; color: black; font-weight: bold; cursor: pointer; } .itinerary-box { margin-top: 25px; padding: 20px; background: #1c1c1c; border-radius: 16px; line-height: 1.45; } .itinerary-line { margin-bottom: 8px; } .itinerary-space { margin-bottom: 14px; } </style> </head> <body> <div id="root"></div> <script type="text/babel"> function formatItinerary(text) { return text .replace(/\n+/g, "\n") .split("\n") .map(line => line.trim() ? <div class='itinerary-line'>${line}</div> : "<div class='itinerary-space'></div>") .join(""); } function App() { const [destination, setDestination] = React.useState(""); const [days, setDays] = React.useState(""); const [result, setResult] = React.useState(""); // Local mock AI function generateItinerary() { const mock = Roteiro para ${destination} - ${days} dias\n\nDia 1 - Chegada e exploração inicial\n• Check-in no hotel\n• Visita ao centro histórico\n• Restaurante recomendado XYZ; setResult(formatItinerary(mock)); } return ( <div className="container"> <h1>Travel Planner</h1> <input type="text" placeholder="Destino" value={destination} onChange={e => setDestination(e.target.value)} /> <input type="number" placeholder="Quantidade de dias" value={days} onChange={e => setDays(e.target.value)} /> <button onClick={generateItinerary}>Gerar Roteiro</button> {result && ( <div className="itinerary-box" dangerouslySetInnerHTML={{ __html: result }} ></div> )} </div> ); } const root = ReactDOM.createRoot(document.getElementById("root")); root.render(<App />); </script> </body> </html>